syntax = "proto3";
package chat;

// Path where the generated Go files will reside
option go_package = "example.com/grpc-demo/pb";

service ChatService {
  rpc SayHello (HelloRequest) returns (HelloReply) {}
}

message HelloRequest { string name = 1; }
message HelloReply { string message = 1; }